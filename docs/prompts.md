1. Initial Propmt

I have a wire-frame ready for a finance management web application. 
[attach image]

I have a base implementation setup in #Day-12&13 in finance-management.

I've built it using the react-vite bundler and I've used react-bootstrap for styling.

For routing I've used react-router.

I want you to implement the following:

1. To standardize the rendering of #Login.tsx and #Signup.tsx 

2. To modify the #NavBar.tsx to a side-bar.tsx

3. Have placeholder components for navigation from side-bar



I want the changes to be precise and minimal and generate a simple explanation report of the changes.
_________________________

2. Achieve exact styling and layout of login and signup page.

1. Ensure that the entire css is similar to the styling of wireframe for all the existing components.

2. Ensure that the #Login.tsx and #Signup.tsx follows exact implementation of the wireframe's Login Screen where it's divided into 2 screen, the left section has info of the product and right section has the login or signup component

________________________

3. Building the accounts list

Focus on screens 3,4 and implement the following:

1. accounts listing page: this contains a table of 4 columns: account name, type, current balance and actions. actions (three dots) are usually deleting the account and updating the account information. Along with this we also have a button add account which create a popup of adding an account.



2. Add account modal:

It has 3 fields: account name, type and current balance. The same model should be re-used when I edit the account information using the listing page.

I want the entire css to follow exactly what's in the wireframe using black and white colors.

__________________-
4. transaction list and edit

Use the below wire-frame to implement transactions. Ensure it's exactly as in the wireframe. This should include the routing to transaction list and add transaction, edit and delete.